# Copy this file to `.env` and fill in real values locally (never commit `.env`).
# The CLI loads this file automatically via python-dotenv.

# ------------------
# OpenAI auth/config
# ------------------
OPENAI_API_KEY=sk-REPLACE_ME             # required: API key for OpenAI or Azure proxy
# OPENAI_BASE_URL=https://api.openai.com/v1   # optional: custom base URL (e.g., Azure/OpenAI proxy)
# OPENAI_ORG_ID=org_...                       # optional: org scoping
# OPENAI_PROJECT_ID=proj_...                   # optional: project scoping

# ------------------
# Model overrides
# ------------------
# OPENAI_MODEL=gpt-4o-mini                      # optional: override default model
# OPENAI_TEMPERATURE=0                          # optional: defaults to 0 in runtime
# OPENAI_MAX_OUTPUT_TOKENS=2000                 # optional
# OPENAI_TIMEOUT_SECONDS=120                    # optional

# ------------------
# Policy bundle
# ------------------
# APS_POLICY_TAG=schemas@v1.0                   # optional: asserts policy tag on runs
# APS_POLICY_ROOT=./contract-analysis-policy-bundle  # optional: bundle path
