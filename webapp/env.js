(() => {
  "use strict";
  // GitHub Pages configuration - no AWS services
  // OpenAI API key will be managed through localStorage
  window._env = {
    APP_VERSION: "2.0.0-github-pages",
    ENVIRONMENT: "github-pages"
  };
})();
