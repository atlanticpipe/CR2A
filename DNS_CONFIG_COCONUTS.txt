
╔════════════════════════════════════════════════════════════╗
║                                                            ║
║  DNS Configuration for coconuts.velmur.info               ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝

Since velmur.info is already configured, you likely have access to its
DNS management. Here's how to add the subdomain:

┌────────────────────────────────────────────────────────────┐
│ CLOUDFLARE CONFIGURATION (Most Common)                     │
└────────────────────────────────────────────────────────────┘

If velmur.info uses Cloudflare:

1. Log into Cloudflare dashboard
2. Select velmur.info domain
3. Go to DNS → Records
4. Click "Add record"
5. Configure:

   ┌─────────────────────────────────────────────────────────┐
   │ Type:    CNAME                                          │
   │ Name:    coconuts                                       │
   │ Target:  [yourusername].github.io                      │
   │ Proxy:   🌥️ DNS only (gray cloud, NOT orange)         │
   │ TTL:     Auto                                           │
   │ Comment: CR2A v2.0 GitHub Pages deployment             │
   └─────────────────────────────────────────────────────────┘

6. Click "Save"

⚠️ IMPORTANT: Use DNS only (gray cloud), not proxied (orange cloud).
   GitHub Pages requires direct DNS resolution for HTTPS to work.

┌────────────────────────────────────────────────────────────┐
│ OTHER DNS PROVIDERS                                        │
└────────────────────────────────────────────────────────────┘

If velmur.info uses another provider:

**Record Configuration:**
- Type: CNAME
- Host/Name: coconuts
- Points to: [yourusername].github.io
- TTL: 3600 (or automatic)

**Provider-Specific Instructions:**

**Route 53 (AWS):**
- Go to Hosted Zones → velmur.info
- Create Record
- Record name: coconuts
- Record type: CNAME
- Value: [yourusername].github.io
- Routing policy: Simple
- Save

**Google Cloud DNS:**
- Go to Cloud DNS → velmur.info
- Add Record Set
- DNS Name: coconuts.velmur.info
- Resource Record Type: CNAME
- Canonical name: [yourusername].github.io
- Create

**Name.com / Namecheap:**
- Go to Domain List → velmur.info → Advanced DNS
- Add New Record
- Type: CNAME Record
- Host: coconuts
- Value: [yourusername].github.io
- TTL: Automatic

┌────────────────────────────────────────────────────────────┐
│ VERIFICATION                                               │
└────────────────────────────────────────────────────────────┘

After adding the DNS record, verify propagation:

**Command Line:**
```bash
# Check CNAME record
dig coconuts.velmur.info CNAME

# Should show:
# coconuts.velmur.info. 300 IN CNAME [yourusername].github.io.

# Check full resolution
dig coconuts.velmur.info

# Should resolve to GitHub Pages IPs:
# 185.199.108.153
# 185.199.109.153
# 185.199.110.153
# 185.199.111.153
```

**Online Tools:**
- https://www.whatsmydns.net/#CNAME/coconuts.velmur.info
- https://dnschecker.org/

**Expected Results:**
✅ CNAME record points to [yourusername].github.io
✅ Resolution shows GitHub Pages IP addresses
✅ All global DNS servers show consistent results

**Propagation Time:**
- Cloudflare: 1-5 minutes
- Other providers: 5-60 minutes
- Full global propagation: Up to 24 hours (rare)

┌────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                            │
└────────────────────────────────────────────────────────────┘

**Problem: DNS not resolving**

Solutions:
1. Wait longer (minimum 5-15 minutes)
2. Clear local DNS cache:
   - macOS: `sudo dscacheutil -flushcache`
   - Windows: `ipconfig /flushdns`
   - Linux: `sudo systemd-resolve --flush-caches`
3. Try different DNS server (8.8.8.8 for testing)
4. Verify CNAME record is correct (no typos)

**Problem: Certificate errors in GitHub Pages**

Causes:
- CNAME record points to wrong target
- Using GitHub.io/repo instead of just GitHub.io
- Cloudflare proxy enabled (should be DNS only)

Solution:
- Verify CNAME target is: [yourusername].github.io (no /repo)
- Disable Cloudflare proxy (gray cloud, not orange)
- Wait 5-10 minutes for GitHub to detect correct DNS

**Problem: Both domains showing same content**

This is expected! Both should work:
- https://velmur.info → CR2A v1.x (main branch)
- https://coconuts.velmur.info → CR2A v2.0 (gh-pages branch)

They're deployed from different branches of the same repo.

┌────────────────────────────────────────────────────────────┐
│ FINAL CHECKLIST                                            │
└────────────────────────────────────────────────────────────┘

Before marking complete:

□ CNAME record added to velmur.info DNS
□ DNS propagation verified (dig command works)
□ GitHub Pages configured with coconuts.velmur.info
□ HTTPS certificate issued (green padlock)
□ Site loads at https://coconuts.velmur.info
□ No console errors (F12)
□ All resources load (CSS, JS, etc.)
□ File upload works
□ OpenAI API calls work (after key configured)
□ PDF export works

┌────────────────────────────────────────────────────────────┐
│ BONUS: Math Joke Explanation                              │
└────────────────────────────────────────────────────────────┘

In category theory (abstract mathematics), 'co-' denotes the dual
of a mathematical concept. Examples:

- Product → Coproduct
- Limit → Colimit
- Algebra → Coalgebra

Taking the dual twice returns you to the original concept, making
the double 'co-' redundant. Therefore:

  coconut = co(co(nut)) = nut

It's a nerdy math joke that works perfectly for a beta testing
subdomain! 🥥 = 🌰

Source: https://en.wikipedia.org/wiki/Mathematical_joke

╔════════════════════════════════════════════════════════════╗
║                                                            ║
║  DNS Configuration Guide Complete!                        ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝
